test:
  clients:
    default:
      database: dataflow_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1

default:
  clients:
    default:
      database: dataflow
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 10
